"""Phase 5 placeholder: the library agent's tool library.

# TODO Phase 5:
# - search_books(query, filters) -> list[BookSummary]
# - get_book_details(book_id) -> BookDetails
# - find_similar(book_id, k=5) -> list[BookSummary]   # uses llama-index + Gemini embeddings
# - summarize_themes(book_ids) -> str
# Each tool: pure async function, typed args/return, no side effects beyond logging.
# Wrap each call in `logfire.span("tools.library.<name>", ...)`.
"""

from __future__ import annotations
